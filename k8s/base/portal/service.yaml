apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: rangemaster
  labels:
    app: portal
spec:
  type: ClusterIP
  selector:
    app: portal
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
