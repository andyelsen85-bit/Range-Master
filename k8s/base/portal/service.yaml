apiVersion: v1
kind: Service
metadata:
  name: portal
  namespace: trapmaster
  labels:
    app: portal
spec:
  type: ClusterIP
  selector:
    app: portal
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
