apiVersion: v1
kind: Service
metadata:
  name: api-server
  namespace: trapmaster
  labels:
    app: api-server
spec:
  type: ClusterIP
  selector:
    app: api-server
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
